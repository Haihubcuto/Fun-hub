local Kavo = loadstring(game:HttpGet("https://raw.githubusercontent.com/KavoUI/Kavo-UI-Library/main/source.lua"))()
local Window = Kavo.CreateLib("Fun Hub - Blox Fruits", "DarkTheme")

-- Tabs
local MainTab = Window:NewTab("Main")
local RaidTab = Window:NewTab("Raid")
local BossTab = Window:NewTab("Boss")
local CombatTab = Window:NewTab("Combat")
local WeaponTab = Window:NewTab("Weapon")
local FruitTab = Window:NewTab("Fruit")

-- Sections
local MainSection = MainTab:NewSection("Auto Farm")
local RaidSection = RaidTab:NewSection("Auto Raid")
local BossSection = BossTab:NewSection("Auto Boss & Summon")
local CombatSection = CombatTab:NewSection("Combat Features")
local WeaponSection = WeaponTab:NewSection("Weapon & Quests")
local FruitSection = FruitTab:NewSection("Auto Fruit")

-- Auto Farm
MainSection:NewToggle("Auto Farm Level", "Tự động farm cấp độ", function(state)
    if state then
        -- Code Auto Farm Level
    else
        -- Dừng Auto Farm Level
    end
end)

MainSection:NewToggle("Auto Farm Bone", "Tự động farm Bone", function(state)
    if state then
        -- Code Auto Farm Bone
    end
end)

MainSection:NewToggle("Auto Farm Katakuri", "Tự động farm Katakuri", function(state)
    if state then
        -- Code Auto Farm Katakuri
    end
end)

MainSection:NewToggle("Auto Farm Katakuri V2", "Tự động farm Katakuri V2", function(state)
    if state then
        -- Code Auto Farm Katakuri V2
    end
end)

-- Auto Raid
RaidSection:NewToggle("Auto Raid", "Tự động Raid", function(state)
    if state then
        -- Code Auto Raid
    end
end)

RaidSection:NewButton("Mua Chip Raid", "Tự động mua Chip Raid", function()
    -- Code Auto Buy Chip
end)

RaidSection:NewToggle("Auto Raid Boss", "Tự động Raid Boss", function(state)
    if state then
        -- Code Auto Raid Boss
    end
end)

-- Auto Boss & Summon
BossSection:NewToggle("Auto Boss", "Tự động đánh Boss", function(state)
    if state then
        -- Code Auto Boss
    end
end)

BossSection:NewButton("Triệu hồi Katakuri V2", "Tự động triệu hồi Katakuri V2", function()
    -- Code Summon Katakuri V2
end)

BossSection:NewButton("Triệu hồi Soul Ripper", "Tự động triệu hồi Soul Ripper", function()
    -- Code Summon Soul Ripper
end)

BossSection:NewButton("Triệu hồi Blackbeard", "Tự động triệu hồi Blackbeard", function()
    -- Code Summon Blackbeard
end)

BossSection:NewButton("Triệu hồi Law", "Tự động triệu hồi Law", function()
    -- Code Summon Law
end)

-- Combat Features
CombatSection:NewToggle("Auto ESP", "Hiển thị ESP", function(state)
    if state then
        -- Code Auto ESP
    end
end)

CombatSection:NewToggle("Auto Aim Bot", "Tự động ngắm mục tiêu", function(state)
    if state then
        -- Code Auto Aim Bot
    end
end)

CombatSection:NewToggle("Auto Fast Attack", "Tấn công nhanh", function(state)
    if state then
        -- Code Auto Fast Attack
    end
end)

CombatSection:NewToggle("Auto Speed Run", "Chạy nhanh", function(state)
    if state then
        -- Code Auto Speed Run
    end
end)

-- Weapon & Quests
WeaponSection:NewToggle("Auto Kaitun", "Tự động Kaitun", function(state)
    if state then
        -- Code Auto Kaitun
    end
end)

WeaponSection:NewToggle("Auto Yama", "Tự động Yama", function(state)
    if state then
        -- Code Auto Yama
    end
end)

WeaponSection:NewToggle("Auto Tushita", "Tự động Tushita", function(state)
    if state then
        -- Code Auto Tushita
    end
end)

WeaponSection:NewToggle("Auto Yama Quest", "Tự động làm nhiệm vụ Yama", function(state)
    if state then
        -- Code Auto Yama Quest
    end
end)

WeaponSection:NewToggle("Auto Tushita Quest", "Tự động làm nhiệm vụ Tushita", function(state)
    if state then
        -- Code Auto Tushita Quest
    end
end)

WeaponSection:NewToggle("Auto CDK", "Tự động lấy Cursed Dual Katana", function(state)
    if state then
        -- Code Auto CDK
    end
end)

-- Auto Fruit
FruitSection:NewToggle("Auto Fruit Collect", "Tự động nhặt trái ác quỷ", function(state)
    if state then
        -- Code Auto Fruit Collect
    end
end)

FruitSection:NewToggle("Auto Store Fruit", "Tự động cất trái ác quỷ", function(state)
    if state then
        -- Code Auto Store Fruit
    end
end)
